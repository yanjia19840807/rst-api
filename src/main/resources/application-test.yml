# JUnit only. Not a runtime environment (runtime is dev / uat / pre / prod).

spring:
  datasource:
    url: "jdbc:h2:mem:rst;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=YEAR,MONTH"
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  security:
    dev-identity:
      override-enabled: true
      ccgid: SUPERVISOR001
      role: SUPERVISOR
      center: GBS CHINA INDIA

rst:
  forecast:
    enabled: false
    base-url: http://localhost:8000
    connect-timeout-ms: 1000
    read-timeout-ms: 1000
    confidence-level: 0.95
  mail:
    enabled: false
    redirect-to:

microsoft:
  graph:
    enabled: false
