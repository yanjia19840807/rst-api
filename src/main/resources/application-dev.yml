app:
  security:
    # Test login. When true, the SPA picks the person (URL / account menu).
    # Unspecified requests default to ADMIN. Keep false after SSO is connected.
    dev-identity:
      override-enabled: true

microsoft:
  graph:
    enabled: true
    client-id: ${MS_GRAPH_CLIENT_ID:2b5ec0be-1344-4161-80f5-b7674d74f019}
    client-secret: ${MS_GRAPH_CLIENT_SECRET:}

# Log SQL with parameters substituted into '?' (dev only). p6spy does this natively.
decorator:
  datasource:
    enabled: true
    p6spy:
      enable-logging: true
      logging: slf4j
      multiline: true
      # %(sql) already has bound values inlined (not separate Params:).
      # Use real \n — p6spy does not treat %n as a newline.
      log-format: "%(executionTime)ms | %(category) | connection %(connectionId)\n%(sql)"
      exclude-categories: info,debug,result,resultset,batch

logging:
  level:
    # Prefer p6spy output; keep Hibernate SQL quiet to avoid duplicates.
    org.hibernate.SQL: warn
    org.hibernate.orm.jdbc.bind: warn
    p6spy: info
    com.cmacgm.gbs.rst.api.forecast: info
    com.cmacgm.gbs.rst.api.graph: info
