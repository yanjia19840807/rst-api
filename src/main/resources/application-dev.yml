spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rst
    username: postgres
    password: "001456"

app:
  security:
    # ACTIVE Timesheet supervisor_ccgid used as the walkthrough identity.
    # Restart API after changing. Optional agent-ccgid for X-Dev-Role: AGENT.
    dev-identity:
      ccgid: S00734522
      role: AGENT
      agent-ccgid: S00734522
      # agent-ccgid: S00734522

logging:
  level:
    org.hibernate.SQL: debug
