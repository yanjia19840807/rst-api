COMMENT ON TABLE support_category IS
    'Standard Production Support categories. Maintained in the database (no app UI). Do not delete rows referenced by exercise_production_support_item; set status to INACTIVE instead.';
